<!--  Copyright (c) Files Community. Licensed under the MIT License.  -->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

	<!--  Material Design 3 Typography Styles  -->

	<Style x:Key="App.Theme.BaseTextBlockStyle" TargetType="TextBlock">
		<Setter Property="FontFamily" Value="{ThemeResource ContentControlThemeFontFamily}" />
		<Setter Property="FontSize" Value="{StaticResource MD3.Typography.Body.Large.Size}" />
		<Setter Property="FontWeight" Value="Medium" />
		<Setter Property="TextTrimming" Value="CharacterEllipsis" />
		<Setter Property="TextWrapping" Value="Wrap" />
		<Setter Property="LineStackingStrategy" Value="MaxHeight" />
		<Setter Property="TextLineBounds" Value="Full" />
	</Style>

	<Style
		x:Key="App.Theme.BodyTextBlockStyle"
		BasedOn="{StaticResource App.Theme.BaseTextBlockStyle}"
		TargetType="TextBlock">
		<Setter Property="FontSize" Value="{StaticResource MD3.Typography.Body.Medium.Size}" />
		<Setter Property="FontWeight" Value="Normal" />
	</Style>

	<Style BasedOn="{StaticResource App.Theme.BaseTextBlockStyle}" TargetType="TextBlock">
		<Setter Property="FontWeight" Value="Normal" />
	</Style>

	<Style
		x:Key="App.Theme.BodyStrongTextBlockStyle"
		BasedOn="{StaticResource App.Theme.BaseTextBlockStyle}"
		TargetType="TextBlock">
		<Setter Property="FontWeight" Value="Medium" />
	</Style>

	<Style
		x:Key="App.Theme.CaptionTextBlockStyle"
		BasedOn="{StaticResource App.Theme.BaseTextBlockStyle}"
		TargetType="TextBlock">
		<Setter Property="FontSize" Value="{StaticResource MD3.Typography.Body.Small.Size}" />
		<Setter Property="FontWeight" Value="Normal" />
	</Style>

	<Style
		x:Key="App.Theme.SubtitleTextBlockStyle"
		BasedOn="{StaticResource App.Theme.BaseTextBlockStyle}"
		TargetType="TextBlock">
		<Setter Property="FontSize" Value="{StaticResource MD3.Typography.Title.Large.Size}" />
		<Setter Property="FontWeight" Value="Normal" />
		<Setter Property="OpticalMarginAlignment" Value="TrimSideBearings" />
	</Style>

</ResourceDictionary>